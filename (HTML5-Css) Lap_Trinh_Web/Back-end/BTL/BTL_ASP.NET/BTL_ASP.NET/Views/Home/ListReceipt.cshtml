@{
    ViewBag.Title = "Danh sách hóa đơn nhập";
}
@using (Ajax.BeginForm("loadTableSearch2", new AjaxOptions { HttpMethod = "Post", UpdateTargetId = "KetQua", InsertionMode = InsertionMode.Replace }))
{
    <div style="padding-left:80%" class="text-muted">
        Mã hóa đơn: @Html.TextBox("txtSearch")
        <input type="submit" value="Search" />
    </div>
}
<h2>Danh sách hóa đơn Nhập</h2>

<div id="KetQua">
    @Html.Action("loadTableListReceipt")
</div>